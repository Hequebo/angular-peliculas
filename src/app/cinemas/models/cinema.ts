export interface CinemaDTO {
    id: number;
    name: string;
    latitude: number;
    length: number;
}

export interface CinemaCreationDTO {
    name: string;
    latitude: number;
    length: number;
}